class Player {
  name: string;
  password: string;
  id: number;

  constructor(name: string, password: string, id: number) {
    this.name = name;
    this.password = password;
    this.id = id;
  }
}

export default Player;
